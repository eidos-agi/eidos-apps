# Eidos Apps — product graph

[`product.json`](product.json) is the canonical OPF v1 graph. This page is a readable projection only.

**Eidos Apps are larger programs. Docker images. Prim-native. Not applets. Not Prim.app. SDK here; first app is Eidos Video.**

```bash
PYTHONPATH=~/repos-eidos-agi/opf python3 -m opf.validate prim.opf
```
